.claude/
/target
*.swp
*.swo
*~
.DS_Store
/pkg
/web/node_modules
/web/dist
/web/pkg
/docs/book
/site
graphify-out/

# Out-of-repo benchmark scratch (symlink into ~/Datasets); pulls a C toolchain
# and OpenMP, deliberately kept outside the crate
/libsais-bench

# Nix build outputs (flake.nix / flake.lock are tracked)
/result
/result-*
.direnv/

# Local dev planning notes (kept out of the repo)
/plan.md
/todo.md
/benchmark-integration-plan.md
